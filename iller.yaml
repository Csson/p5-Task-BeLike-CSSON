---
doctype: dist

header:
  name: Task-BeLike-CSSON

prereqs:
  runtime:
    requires:
      - perl: 5.010001

plugins:
 - +config: Author::CSSON
   is_task: 1

---
doctype: weaver

plugins:
  - +config: Author::CSSON

  - +replace_plugin: Badges
    formats: html, markdown
    badge:
     - perl
     - travis
